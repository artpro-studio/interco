export enum ITypeAttributes {
    TEXT = 'text',
    NUBMER = 'number',
    DATE = 'date',
    DATETIME = 'datetime',
    EDITOR = 'editor',
    TEXTAREA = 'textarea',
    FILE = 'file',
    IMAGE = 'image',
    GALLARY = 'gallary',
    ARRAY = 'array',
}
