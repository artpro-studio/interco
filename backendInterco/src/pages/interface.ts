export enum PagesType {
    LANDING = 'Landing',
    BLOG = 'blog',
}

export enum CommentStatus {
    VERIFY = 'verify',
    SUCCESS = 'success',
}

export enum ITypePagesParams {
    TEXT = 'text',
    NUBMER = 'number',
    DATE = 'date',
    DATETIME = 'datetime',
    EDITOR = 'editor',
    TEXTAREA = 'textarea',
    FILE = 'file',
    IMAGE = 'image',
    GALLARY = 'gallary',
}
